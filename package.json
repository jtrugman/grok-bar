{
  "name": "search-with-grok",
  "version": "1.1.0",
  "description": "Chrome extension: use Grok as your default address-bar search (no keyword required)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "validate": "node scripts/validate-manifest.js",
    "check": "node --test tests/**/*.test.js && node scripts/validate-manifest.js"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "chrome-extension",
    "omnibox",
    "grok",
    "xai",
    "chatgpt",
    "claude",
    "perplexity"
  ],
  "license": "MIT"
}
